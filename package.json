{
  "name": "@poc/root",
  "private": true,
  "devDependencies": {
    "craco": "^0.0.3",
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "lerna exec --scope @poc/employees -- yarn start"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  }
}
